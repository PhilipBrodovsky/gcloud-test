# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: client-soccer-manager cicd
"on":
  push:
    branches:
      - main
    paths:
      - "client-soccer-manager/**"
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          cd client-soccer-manager
          ls
          npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          entryPoint: ./client-soccer-manager
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_EASYLIFE_55A85 }}"
          channelId: soccer-manager
          projectId: easylife-55a85
          target: soccer-manager
